{
  "default": {
    "require": [
      "tests/step_definitions/hooks.ts",
      "tests/step_definitions/**/*.ts"
    ],
    "requireModule": ["ts-node/register"],
    "format": ["progress"],
    "paths": ["tests/features/**/*.feature"],
    "formatOptions": {
      "snippetInterface": "async-await"
    }
  }
}
