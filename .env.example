PORT=4000
NODE_ENV=development

# Conectar al contenedor db
DATABASE_URL="postgresql://postgres:postgres@db:5432/hora_vital_dev?schema=public"

# Seguridad
JWT_SECRET=changeme_in_dev_use_secure_secret_in_prod
JWT_EXPIRES_IN=24h
BCRYPT_SALT_ROUNDS=12

# Seed Configuration
SEED_DEFAULT_PASSWORD=Password123!

# CORS Configuration - Lista blanca de dominios permitidos (separados por coma)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://localhost:3000,https://localhost:3001,https://yourdomain.com

# Email Configuration
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=no-reply@horavital.com

